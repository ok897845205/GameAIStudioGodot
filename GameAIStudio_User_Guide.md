# GameAIStudio 用户使用手册

本文档面向第一次使用 GameAIStudio 的用户，从安装后的首次打开开始，完整说明如何配置环境、创建游戏、让 AI 团队继续制作、预览、导出、管理版本，以及排查常见问题。

## 1. GameAIStudio 是什么

GameAIStudio 是一个桌面端 AI 游戏创作工具。你可以用自然语言描述一个 2D 或 3D 游戏，软件会基于 Godot 模板创建项目，并调用本机 AI CLI 组成一个多 Agent 团队来完成策划、美术、程序、QA、Web 导出和预览。

典型使用流程是：

1. 打开软件，确认目录和环境。
2. 配置至少一个可用的本地 AI CLI。
3. 点击“新建游戏”，输入游戏名称和需求。
4. 等待 AI 团队生成可玩的 Godot 项目。
5. 在软件内预览、继续制作、修复错误或优化美术。
6. 导出 Web zip，交给别人试玩或发布。

## 2. 首次使用前需要准备什么

### 2.1 电脑环境

建议在 Windows 上使用。软件内置 Godot 运行时和模板，但仍需要本机有基础开发环境，尤其是：

- Git：用于保存和还原项目版本。
- Node.js：用于部分 CLI、构建和辅助工具。
- 至少一个本地 AI CLI：用于实际执行 AI Agent。

软件设置页会自动检测这些工具。缺失时，可以在“设置 · 环境诊断”里点击对应的“安装”按钮。Git 和 Node.js 会通过 winget 安装，过程可能需要几分钟。

### 2.2 支持的本地 AI CLI

GameAIStudio 会扫描这些本地 AI CLI：

- KSCC
- Codex
- Claude
- Kimi
- Gemini
- Qwen Code
- Cursor
- Copilot

只安装 CLI 还不够，你还需要按对应工具的要求完成登录或配置密钥。完成后，在 GameAIStudio 的“设置 · 环境诊断”中点击“测试”，确认状态可用。

### 2.3 Godot 不需要单独安装

GameAIStudio 自带 Godot 引擎和 2D/3D 模板。正常情况下，你不需要另外下载 Godot。设置页的“Godot 运行时”区域会显示内置 Godot 的状态、版本和模板检查结果。

## 3. 第一次打开软件

首次打开时，软件会自动弹出“首次配置 · 设置”窗口。

你需要确认两个目录：

- 软件数据目录：保存软件状态、App 日志、全局配置等维护数据。
- 游戏项目目录：以后新建的游戏项目会放在这里。

两项都可以留空使用默认位置。默认用户项目目录通常在：

```text
%USERPROFILE%\Documents\GameAIStudio\projects
```

如果你希望像示例一样把项目放到 `E:\game`，可以在“游戏项目目录”里选择该目录。保存后，软件可能提示重启，按提示重启即可。

注意：

- 修改目录设置不会自动搬迁已有项目。
- 如果配置的目录在启动时不可用，例如移动硬盘未连接，软件会临时使用默认目录，恢复目录后重启即可。
- 软件数据目录和游戏项目目录建议放在稳定磁盘，不建议放在会自动清理的临时目录。

## 4. 环境诊断与 AI CLI 配置

点击左下角“设置”，进入“设置 · 环境诊断”。

### 4.1 查看基础环境

在设置页中检查：

- Git 是否已安装。
- Node.js 是否已安装。
- Godot 运行时是否正常。
- 2D/3D 模板是否可用。

如果 Git 或 Node.js 缺失，点击对应“安装”。安装完成后，软件会自动刷新检测结果。

### 4.2 查看本地 AI CLI

“本地 AI CLI”区域会列出各 AI CLI 的状态。

你会看到这些信息：

- 已安装或未安装。
- Auth 状态。
- 模型状态。
- 额度状态。
- 是否支持图片输入。
- 是否支持 ACP 模式。

推荐操作顺序：

1. 先安装或配置你准备使用的 CLI。
2. 回到 GameAIStudio 设置页。
3. 点击“测试”，确认登录和连接可用。
4. 如果某个 CLI 支持 ACP 但未启用，可点击“安装 ACP”启用更好的流式和工具调用体验。

如果所有 AI CLI 都不可用，软件可以打开项目，但无法让 Agent 执行生成、修改、修复等工作。

## 5. 创建第一个游戏

### 5.1 打开新建窗口

在左侧栏点击“新建游戏”。如果当前没有项目，主区域也会显示“创建第一个 Godot 游戏”的入口。

### 5.2 填写游戏名称

“游戏名称”只用于软件内显示和导出文件命名，可以使用中文，例如：

```text
采集游戏
```

实际项目目录会自动生成纯英文路径，例如：

```text
2D_game_20260615150021
```

这样做是为了避免部分 AI CLI、脚本工具或 Godot 导出流程遇到中文路径兼容问题。

### 5.3 选择游戏维度

选择 2D 或 3D。选择后，软件会复制对应的 Godot 模板。

如果你不确定，建议从 2D 开始。2D 游戏生成速度更快，也更适合首次验证流程。

### 5.4 写好“一句话需求”

这里可以写一句话，也可以写一段较完整的需求。越具体，AI 团队越容易一次做出接近目标的版本。

推荐包含这些信息：

- 游戏类型：平台跳跃、射击、采集、塔防、解谜等。
- 玩家操作：移动、跳跃、射击、拖拽、点击等。
- 核心目标：得分、通关、限时、生存、击败敌人等。
- 关卡或场景：森林、太空、地牢、城市、海底等。
- 失败条件：生命值耗尽、时间结束、敌人碰撞等。
- 美术风格：像素风、手绘风、科幻、童话、极简等。
- 音效和音乐需求：BGM、采集音效、受伤音效、胜利音效等。

示例：

```text
创建一个 2D 休闲采集游戏。玩家在森林地图中移动，收集蘑菇、浆果和金币，同时躲避史莱姆和陷阱。玩家需要在 90 秒内达到目标分数。不同蘑菇有不同分值，毒蘑菇会扣分。游戏包含教程提示、分数、时间、生命值、胜利和失败界面。美术风格是温暖童话手绘风，生成角色、蘑菇、浆果、金币、史莱姆、草地、树木、陷阱、UI，以及采集、受伤、胜利、失败音效和轻松背景音乐。
```

### 5.5 选择 Agent CLI

新建窗口会显示“Agent CLI”。GameAIStudio 的团队默认包含：

- 制作人：拆目标、定范围、控制风险。
- 策划：设计玩法、规则、关卡、数值。
- 美术：定义风格、素材清单、占位或生成素材。
- 程序：修改 Godot 脚本、场景、导出配置。
- QA：验证可玩性、导出、缺陷和验收。

每个角色都可以选择不同的本地 AI CLI。初次使用时，建议所有角色先使用同一个已经测试可用的 CLI，减少变量。

如果界面提示“需要修复”，说明某些角色选择的 CLI 不可用。需要先到设置页安装、登录或测试。

### 5.6 创建并启动团队工作流

确认后点击“创建并启动团队工作流”。

软件会自动完成这些动作：

1. 复制 2D 或 3D Godot 模板到新项目目录。
2. 初始化项目说明文件和 Agent 上下文。
3. 为项目初始化 Git 版本管理。
4. 启动制作人、策划、美术、程序、QA 等 Agent。
5. 根据 Agent 结果修改项目文件。
6. 尝试 Web 导出、产物检查、zip 打包和预览刷新。
7. 把运行日志和项目状态写入项目目录。

运行中不要同时对同一个项目发送新消息。如果需要停止，点击运行提示里的“取消”。

## 6. 主界面怎么用

创建项目后，界面大致分为四块：

- 左侧项目列表：切换项目、新建游戏、打开 AI 生图、设置。
- 中间对话区：和自动模式或某个 Agent 对话。
- 顶部项目工具条：打开目录、浏览器预览、切换 Agent CLI。
- 右侧预览和工具面板：Web 预览、构建、运行、Git、状态。

### 6.1 项目列表

左侧会显示所有已创建项目。每个项目会显示：

- 项目名称。
- 2D 或 3D。
- 最近更新时间。
- 是否正在运行。

点击项目即可切换。不同项目可以分别保存自己的聊天记录、Git 状态和导出结果。

### 6.2 中间对话区

对话区用于继续让 AI 修改游戏。你可以：

- 让自动模式判断该找哪个 Agent。
- 手动切换到制作人、策划、美术、程序或 QA。
- 发送文字需求。
- 附加图片，让支持图片输入的 CLI 使用。
- 清空当前会话或导出聊天记录。

常见指令示例：

```text
让玩家移动速度提高 20%，并让金币更容易看见。
```

```text
现在敌人太多了，请降低前 30 秒难度，后面再逐步增加。
```

```text
请检查 Web 导出失败的原因并直接修复。
```

```text
请把 UI 调整得更适合 16:9 浏览器窗口，避免文字重叠。
```

### 6.3 快捷操作条

项目没有运行任务时，对话区上方会出现快捷操作：

- 继续制作：再跑一轮团队工作流。
- 修复错误：让程序 Agent 检查报错、导出失败和明显缺陷。
- QA 测试：让 QA Agent 验证当前版本。
- 美术优化：让美术 Agent 优化视觉表现。
- 浏览器预览：启动或打开 Web 实时预览。
- 导出 Web：导出 Web zip。
- 保存版本：提交当前 Git 变更。
- 查看日志：打开项目维护日志预览。

如果你不确定下一步做什么，优先用“QA 测试”或“继续制作”。

## 7. Web 实时预览

右侧栏顶部是“Web 实时预览”。

点击“启动”后，软件会启动本地预览服务，加载项目的 `build/web/index.html`。如果项目文件变化，软件会尝试刷新预览。

你也可以点击顶部“浏览器预览”，在系统浏览器里打开当前预览地址。

预览地址通常类似：

```text
http://127.0.0.1:60037/index.html?v=...
```

这个地址是本机临时地址，主要用于开发和试玩，不适合作为最终发布链接。

如果预览失败，常见原因是：

- 还没有成功生成 `build/web/index.html`。
- Godot Web 导出失败。
- 当前项目仍在运行任务，预览产物尚未刷新。

此时可以在右侧“构建”中重新执行 Web 导出，或点击“修复错误”让程序 Agent 检查。

## 8. 构建与导出

右侧栏切到“构建”。

### 8.1 Web 导出

点击“Web 导出”，软件会调用 Godot Web 导出流程，把游戏导出到：

```text
<项目目录>\build\web
```

成功后，`build/web` 中通常包含：

- `index.html`
- `index.js`
- `index.wasm`
- `index.pck`
- 图标和其他 Web 运行文件
- `gameaistudio-export.json`

示例项目中，Web 构建目录是：

```text
E:\game\2D_game_20260615150021\build\web
```

### 8.2 导出 zip

点击“导出 zip”，软件会重新执行 Godot Web 导出，并把 `build/web` 打包成 zip。

zip 通常输出到：

```text
<项目目录>\dist\<游戏名称>-web.zip
```

示例项目的导出包是：

```text
E:\game\2D_game_20260615150021\dist\采集游戏-web.zip
```

导出完成后，右侧“构建”面板会显示“最新 Web zip”，点击“打开 zip”即可在系统中打开。

### 8.3 什么时候需要重新导出

以下情况建议重新导出 zip：

- 让 Agent 修改了脚本、场景、素材或 UI。
- 替换了 AI 生成的图片或音频。
- 修复了 QA 发现的问题。
- 想把最新版本发给别人试玩。

仅在软件内预览不等于最终发布。要交付给别人，请使用“导出 zip”。

## 9. AI 素材与音频

GameAIStudio 提供两个素材入口：

- “AI 生图”：打开马良画卷网页，可生成图片后下载，再拖入聊天发给 Agent 使用。
- “AI 素材”：为当前项目生成图片或音频，自动保存到项目素材库，并给 Agent 提供 `res://` 路径。

### 9.1 AI 生图

点击左侧“AI 生图”，会打开“AI 生图 · 马良画卷”。

它适合快速做单张参考图或灵感图。生成图片后，你可以下载到本机，然后拖入 GameAIStudio 的聊天区，发给支持图片输入的 Agent。

如果当前 CLI 不支持图片输入，发送图片时会提示切换 CLI 或移除图片。

### 9.2 AI 素材工坊

选择一个项目后，点击顶部“AI 素材”。

素材工坊包含：

- 图片：输入提示词生成项目图片素材。
- 音频：生成 BGM、音效或环境音。
- 服务设置：配置图片和音频服务商。
- 素材库：查看、刷新、重生成、删除和绑定槽位。

生成的图片会保存到项目的素材目录中，并记录到：

```text
<项目目录>\.gameaistudio\asset-manifest.json
```

示例项目的素材包括：

```text
E:\game\2D_game_20260615150021\assets\backgrounds
E:\game\2D_game_20260615150021\assets\characters
E:\game\2D_game_20260615150021\assets\images
E:\game\2D_game_20260615150021\assets\ui
```

音频会保存到：

```text
<项目目录>\assets\audio
```

示例项目中，背景音乐位于：

```text
E:\game\2D_game_20260615150021\assets\audio\bgm
```

### 9.3 服务设置

在“AI 素材”里的“服务设置”配置图片或音频服务商。

图片服务商需要填写：

- 名称。
- 协议。
- API 基址。
- API Key。
- 可选额外请求头。

图片模型与路由用于设置某个模型应该调用哪些服务商。多个服务商可按顺序降级，前一个失败时自动尝试下一个。

音频服务商当前支持 ACE Music 等文生音频服务。需要填写：

- 名称。
- API 基址。
- 上游模型 ID。
- 输出格式。
- API Key。

服务商 API Key 只保存在本机。配置后建议点击“测试”，确认连接正常。

### 9.4 槽位是什么

素材库中的“槽位”用于告诉 Agent 这张图或这段音频在游戏里扮演什么角色。例如：

```text
player.main
enemy.basic
item.coin
world.background
ui.button
bgm.main
sfx.collect
```

有槽位的素材会在 Agent 上下文中更明确，程序 Agent 更容易把正确素材接入场景。

如果你对生成结果不满意，可以点击“重新生成”。重新生成会替换原文件，但保持 `res://` 路径和槽位不变。这样已经接入代码或场景的资源引用不会失效。

### 9.5 团队工作流中的自动素材

图片素材闭环默认参与团队工作流。美术 Agent 会规划素材，系统会尝试生成图片并交给程序和 QA 使用。

音频是可选项。需要在“AI 素材”里的音频服务设置中打开：

```text
创建游戏的团队工作流中自动生成音频（1 首 BGM + 核心音效）
```

如果未配置图片或音频服务，基础游戏生成仍可以继续，但可能使用占位素材或由 Agent 给出后续替换建议。

## 10. Git 版本管理

GameAIStudio 会使用 Git 管理项目版本。右侧切到“Git”可以查看和操作。

你可以看到：

- 当前分支。
- 当前 HEAD。
- 未提交变更。
- 最近提交。
- 提交输入框。
- 还原到指定提交的入口。

### 10.1 保存版本

当项目有未提交变更时，输入提交信息，点击“提交”。也可以使用快捷操作“保存版本”。

建议在这些节点保存：

- 初始游戏可以运行。
- 完成一次重要玩法修改。
- 替换了大量素材。
- QA 测试通过。
- 导出 zip 前。

### 10.2 还原版本

在最近提交列表中点击“还原”，或输入提交 hash 后点击“还原”。

注意：

- 还原会覆盖当前未提交的本地改动。
- 如果界面提示有未提交变更，请先确认是否需要保存。
- `.gameaistudio` 中的内部状态、聊天和日志不会作为普通游戏版本一起还原。

## 11. 状态面板与项目维护

右侧切到“状态”，可以查看当前项目的维护信息。

### 11.1 Agent CLI 配置

“Agent CLI 配置（团队工作流使用）”用于设置每个角色默认使用的本地 AI CLI。

如果某个角色经常执行失败，可以在这里切换到另一个已测试可用的 CLI。修改后，下次团队工作流或对应 Agent 回合会使用新的配置。

### 11.2 项目文件预览

状态面板提供几个常用文件入口：

- 项目说明：预览 `GAMEAISTUDIO.md`。
- Agent 上下文：预览 `docs/agent-context.md`。
- Agent 日志：预览 `.gameaistudio/agent-journal.md`。
- 项目日志：预览 `.gameaistudio\logs\project.log`。

这些文件能帮助你理解 AI 团队做了什么、当前项目有哪些资源和交接记录。

### 11.3 App 日志和聊天记录

状态面板可打开 `app.log`，也可以导出当前项目聊天记录为 Markdown。

当你需要把问题反馈给开发者或团队成员时，建议一起提供：

- 当前项目目录。
- 导出的聊天记录。
- 项目日志。
- App 日志。
- 你看到的错误提示截图或文字。

### 11.4 删除项目

右侧底部有“删除项目”按钮。

删除项目会把本地游戏目录一起删除，例如：

```text
E:\game\2D_game_20260615150021
```

这是不可撤销操作。删除前请确认：

- 已导出需要保留的 Web zip。
- 已备份需要保留的项目文件。
- 当前项目没有正在运行的任务。

## 12. 项目文件结构

每个游戏都是一个完整的 Godot 项目。以示例项目为例：

```text
E:\game\2D_game_20260615150021
```

常见目录说明：

```text
.gameaistudio        GameAIStudio 内部状态、聊天、日志、素材清单
.git                 Git 版本库
.godot               Godot 本地导入缓存
addons               Godot 插件，例如测试插件
assets               图片、音频、字体等游戏素材
build\web            Godot Web 导出产物，预览使用
dist                 Web zip 发布包
docs                 AI 团队生成的策划、美术、QA 等文档
export               Web 导出模板相关文件
scenes               Godot 场景
scripts              GDScript 脚本
tests                Godot 测试
tools                项目工具脚本
```

常见文件说明：

```text
project.godot        Godot 项目配置
export_presets.cfg   Godot 导出预设
GAMEAISTUDIO.md      项目说明和 Agent 协作规则
.gitignore           Git 忽略规则
```

`.gameaistudio` 中常见文件：

```text
project.json         项目状态、预览地址、导出路径等
chat-history.json    项目聊天记录
agent-journal.md     Agent 运行交接日志
asset-manifest.json  AI 生成素材和音频记录
logs\project.log     项目维护日志
logs\agents\*.log    各 Agent 日志
```

一般用户不需要手动修改 `.gameaistudio`。如果要查看项目状态或排查问题，可以在软件右侧“状态”或快捷操作“查看日志”中预览。

`docs` 目录适合查看 AI 团队生成的文档。示例项目中包含：

```text
producer-scope.md
gameplay-design.md
art-direction.md
qa-step5-report.md
agent-context.md
```

## 13. 打开和编辑项目

### 13.1 打开项目目录

在项目顶部工具条点击“打开目录”，可以在系统文件管理器中打开当前 Godot 项目。

### 13.2 用 Godot 打开

如果你需要手动编辑场景、脚本或导出设置，可以使用软件提供的 Godot 打开能力，或在文件管理器中用 Godot 打开项目的 `project.godot`。

建议：

- 普通用户优先通过 GameAIStudio 对话让 Agent 修改。
- 熟悉 Godot 的用户可以手动编辑，再回到 GameAIStudio 中预览、导出、保存版本。
- 手动修改后，建议点击右侧 Git 的“刷新 Git 状态”查看变更。

## 14. 聊天记录、日志和排查资料

### 14.1 导出聊天记录

右侧“状态”中点击“导出聊天记录”，可以把项目聊天记录导出为 Markdown，便于排查问题或分享给团队。

### 14.2 查看项目日志

点击快捷操作“查看日志”，或在右侧“状态”中预览项目日志。

项目日志通常位于：

```text
<项目目录>\.gameaistudio\logs\project.log
```

App 维护日志通常位于软件数据目录下：

```text
<软件数据目录>\logs\app.log
```

当 AI CLI 执行失败、导出失败、预览失败或更新失败时，日志是最重要的排查材料。

## 15. 软件更新

点击左下角版本号或设置页里的“软件更新”，可以查看更新信息。

更新状态可能包括：

- 当前已是最新版本。
- 可选迭代更新。
- 必须更新。
- 正在下载。
- 安装器已启动。
- 更新检查失败。

如果是小版本、大版本或服务器要求的强制更新，软件会要求先完成更新，之后才能继续创建游戏、运行团队工作流或发送 Agent 对话。

更新过程中不要强行关闭软件。下载和安装失败时，查看 `app.log`。

## 16. 常见问题

### 16.1 点击“创建并启动团队工作流”不可用

常见原因：

- 没有填写“一句话需求”。
- 某些 Agent CLI 不可用。
- 软件检测到必须更新版本。
- 当前软件正在执行其他任务。

处理方式：

1. 补充需求描述。
2. 到设置页修复 AI CLI。
3. 如果有强制更新，先完成更新。
4. 等待当前任务结束，或取消当前任务。

### 16.2 AI CLI 显示已安装但不可用

可能原因：

- 未登录。
- API Key 或账号状态异常。
- 额度不足。
- CLI 不在 PATH 中。
- 网络或代理配置异常。

处理方式：

1. 在系统终端中运行该 CLI，确认能正常对话或执行。
2. 回到 GameAIStudio 设置页点击“测试”。
3. 根据测试结果修复登录、额度或路径问题。

### 16.3 图片发送失败

原因通常是当前 CLI 不支持图片输入。

处理方式：

- 切换到支持图片的 CLI。
- 移除图片，只发送文字需求。
- 先把图片放到项目素材目录，再告诉 Agent 文件路径。

### 16.4 Web 预览空白或打不开

处理方式：

1. 右侧“构建”点击“Web 导出”。
2. 确认 `build/web/index.html` 存在。
3. 点击“浏览器预览”重新打开。
4. 如果仍失败，点击“修复错误”让程序 Agent 检查导出和 Web 运行问题。
5. 查看项目日志。

### 16.5 导出 zip 失败

常见原因：

- Godot Web 导出失败。
- `build/web` 缺少 `index.html`、`.wasm` 或 `.pck`。
- 当前项目文件被其他程序占用。
- 项目仍在运行任务。

处理方式：

1. 等待或取消当前任务。
2. 重新点击“导出 zip”。
3. 让程序 Agent 执行“修复错误”。
4. 查看 `.gameaistudio\logs\project.log`。

### 16.6 生成的游戏不符合预期

这是正常迭代过程。建议用更具体的反馈继续制作：

```text
保留当前玩法，但把目标改成 120 秒内收集 50 分。减少陷阱数量，增加金币吸附范围。
```

```text
现在画面太暗，请把背景、角色和可收集物做得更明亮，并提高 UI 对比度。
```

```text
请按 QA 视角测试当前版本，列出 5 个最影响试玩的问题，并直接修复前 3 个。
```

### 16.7 我可以手动改项目文件吗

可以。GameAIStudio 生成的是标准 Godot 项目。

但建议遵守：

- 手动改动前先保存 Git 版本。
- 不要手动编辑 `.gameaistudio` 内部状态文件。
- 修改后回到软件中刷新 Git 状态，并重新预览或导出。

## 17. 推荐工作流

### 17.1 第一次生成试玩版

1. 设置游戏项目目录。
2. 确认一个 AI CLI 可用。
3. 新建 2D 游戏。
4. 输入完整需求。
5. 等待团队工作流结束。
6. 点击“浏览器预览”试玩。
7. 点击“QA 测试”。
8. 点击“修复错误”。
9. 点击“导出 zip”。

### 17.2 做一轮内容迭代

1. 在浏览器预览中试玩。
2. 写下最想改的 1 到 3 个点。
3. 在对话区发送明确需求。
4. 等 Agent 完成。
5. 重新预览。
6. 保存 Git 版本。
7. 导出新 zip。

### 17.3 做素材替换

1. 打开“AI 素材”。
2. 在“服务设置”中确认图片或音频服务商可用。
3. 进入“图片”或“音频”生成新素材。
4. 在“素材库”给素材设置槽位。
5. 如果已有素材接入游戏，用“重新生成”替换，保持路径不变。
6. 让程序 Agent 检查素材是否接入正确。
7. 预览并导出。

## 18. 示例项目参考

你提供的示例目录：

```text
E:\game\2D_game_20260615150021
```

它是一个 2D 采集游戏项目。项目名称为“采集游戏”，原始需求包含森林采集、蘑菇、浆果、金币、史莱姆、陷阱、90 秒目标分数、教程提示、胜利失败界面、手绘童话风格、音效和背景音乐。

该项目生成后包含：

- 完整 Godot 项目文件。
- AI 团队文档：`docs\producer-scope.md`、`docs\gameplay-design.md`、`docs\art-direction.md`、`docs\qa-step5-report.md`。
- 图片素材：背景、角色、陷阱、UI 等。
- 音频素材：`assets\audio\bgm` 中的 BGM。
- Web 构建：`build\web`。
- Web 发布包：`dist\采集游戏-web.zip`。
- GameAIStudio 状态和日志：`.gameaistudio`。

这就是一次完整团队工作流结束后，用户可以期待看到的项目形态。

## 19. 给新用户的提示

- 第一次不要追求一次生成完美游戏，先生成可玩的最小版本。
- 每次只让 AI 修改少量明确问题，成功率更高。
- 经常预览，少量多次迭代。
- 重要节点使用 Git 保存版本。
- 导出给别人前，一定先自己用浏览器试玩一次。
- 日志不是普通用户每天都要看，但遇到失败时很有用。
- 如果 AI 输出不稳定，优先检查 CLI 登录、额度和模型状态。

完成以上流程后，你就可以从一句话需求开始，逐步生成、试玩、修改并导出一个可分享的 Godot Web 游戏。
